-- godlygeek/tabular, carried over from the old vimrc for :Tabularize alignment.
-- The <leader>a* mappings live in lua/config/keymaps.lua; it lazy-loads on cmd.
return {
  "godlygeek/tabular",
  cmd = "Tabularize",
}
